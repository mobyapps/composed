### `pecl install tensor`
- gfortran                        \
- libopenblas-dev                 \
- liblapacke-dev                  \

### TODO
- `--with-openssl-argon2`: configure: error: argon2 hashing requires OpenSSL 3.2

# 安装 Microsoft Core Fonts（包含 Arial），安装过程会弹窗确认同意协议
`apt install ttf-mscorefonts-installer`
